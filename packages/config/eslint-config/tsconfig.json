{
  "extends": "@kubb/ts-config/base.json",
  "compilerOptions": {
    // "composite": true,
    // "rootDir": ".",
    // "outDir": "./dist",
    // "tsBuildInfoFile": "./build/.tsbuildinfo"
    // add paths also here because when set with `base.json` it will use the `./dist` folder instead of `./src`.
    "paths": {
      "@kubb/tsup-config": [
        "../tsup-config/src/index.ts"
      ],
    }
  },
  "include": [
    "src/**/*",
    "./package.json",
    "./tsup.config.ts"
  ],
  "exclude": [
    "**/node_modules",
    "**/types/**"
  ],
  // "references": [
  // ]
}