{
  "type": "object",
  "allOf": [
    { "$ref": "#/components/schemas/CounterPartyIncomingTransfer" },
    {
      "required": ["id", "type"],
      "properties": {
        "type": { "type": "string", "enum": ["BANK"], "x-readme-ref-name": "CounterPartyIncomingTransferType" },
        "id": {
          "type": "string",
          "description": "\nThe financial account id: Can be found from the [List linked accounts](/openapi/payments_api/#operation/listLinkedAccounts) endpoint\n"
        }
      }
    }
  ],
  "properties": { "type": { "enum": ["BANK"] } },
  "required": ["type"],
  "x-readme-ref-name": "CounterPartyBankDetails"
}
