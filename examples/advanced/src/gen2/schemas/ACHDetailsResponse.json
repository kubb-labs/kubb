{
  "type": "object",
  "allOf": [
    { "$ref": "#/components/schemas/PaymentAccountDetailsResponse" },
    {
      "required": ["account_number", "payment_instrument_id", "routing_number", "type"],
      "properties": {
        "type": { "type": "string", "enum": ["ACH", "DOMESTIC_WIRE", "CHEQUE", "INTERNATIONAL_WIRE"], "x-readme-ref-name": "PaymentDetailsTypeResponse" },
        "payment_instrument_id": {
          "type": "string",
          "description": "Payment Instrument ID that can be passed to the /transfers endpoint to trigger a transfer.\nThe type of the payment instrument dictates the method.\n"
        },
        "routing_number": { "type": "string" },
        "account_number": { "type": "string" },
        "account_type": { "nullable": true, "allOf": [{ "type": "string", "enum": ["CHECKING", "SAVING"], "x-readme-ref-name": "AccountType" }] },
        "account_class": { "nullable": true, "allOf": [{ "type": "string", "enum": ["BUSINESS", "PERSONAL"], "x-readme-ref-name": "AccountClass" }] }
      }
    }
  ],
  "properties": { "type": { "enum": ["ACH"] } },
  "required": ["type"],
  "x-readme-ref-name": "ACHDetailsResponse"
}
