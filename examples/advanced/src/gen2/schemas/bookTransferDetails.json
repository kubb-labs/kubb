{
  "type": "object",
  "allOf": [
    { "$ref": "#/components/schemas/CounterParty" },
    {
      "required": ["recipient", "type"],
      "properties": {
        "type": { "type": "string", "enum": ["VENDOR", "BOOK_TRANSFER"], "x-readme-ref-name": "CounterPartyType" },
        "recipient": {
          "required": ["id", "type"],
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "description": "Specifies the type of the recipient. \n`ACCOUNT_ID` is the ID of a Brex Business account.\n`PAYMENT_INSTRUMENT_ID` is the ID of Payment Instrument of the receiving Brex account.\n",
              "enum": ["ACCOUNT_ID", "PAYMENT_INSTRUMENT_ID"],
              "x-readme-ref-name": "RecipientType"
            },
            "id": { "type": "string" }
          },
          "x-readme-ref-name": "Recipient"
        }
      }
    }
  ],
  "properties": { "type": { "enum": ["BOOK_TRANSFER"] } },
  "required": ["type"],
  "x-readme-ref-name": "BookTransferDetails"
}
