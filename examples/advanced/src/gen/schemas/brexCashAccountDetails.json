{
  "type": "object",
  "allOf": [
    { "$ref": "#/components/schemas/OriginatingAccount" },
    {
      "required": ["id", "type"],
      "properties": {
        "type": { "type": "string", "enum": ["BREX_CASH"], "x-readme-ref-name": "OriginatingAccountType" },
        "id": {
          "type": "string",
          "description": "\nID of the Brex Business account: Can be found from the `/accounts` endpoint\nwhere instrument type is `CASH`.\n"
        }
      }
    }
  ],
  "x-readme-ref-name": "BrexCashAccountDetails"
}
