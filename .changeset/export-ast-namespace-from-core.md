---
'@kubb/core': minor
---

Export `ast` namespace from `@kubb/core`.

Community plugins can now import `{ ast }` from `@kubb/core` instead of depending on `@kubb/ast` directly. All AST types are also re-exported.
