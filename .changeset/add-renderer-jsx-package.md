---
"@kubb/renderer-jsx": minor
"@kubb/core": patch
---

Add `@kubb/renderer-jsx`, a small JSX renderer for Kubb plugins with a custom JSX runtime, `createRenderer`, and built-in components (`File`, `Const`, `Function`, `Type`, `Root`). Replaces `@kubb/react-fabric` as the rendering layer.
