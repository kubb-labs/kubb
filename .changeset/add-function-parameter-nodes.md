---
"@kubb/ast": minor
---

Add AST nodes for function parameters.

New types: `FunctionParameterNode`, `FunctionParametersNode`, `ObjectBindingParameterNode`.

New `functionPrinter` supports `declaration`, `call`, `keys`, and `values` rendering modes.
