---
'@kubb/ast': patch
---

Update the README to match the current export map. The imports table drops the removed `@kubb/ast/utils`, `@kubb/ast/macros`, and `kubb/ast` paths, and a note explains that `@kubb/ast` is the standalone install for AST-only usage while most Kubb code reaches the same surface through the `ast` namespace from `kubb/kit`.
