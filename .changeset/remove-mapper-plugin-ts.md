---
"@kubb/plugin-ts": major
---

Remove the `mapper` option from `@kubb/plugin-ts`. This option allowed overriding specific TypeScript property signatures by name. Use a custom generator instead to transform the generated output.
