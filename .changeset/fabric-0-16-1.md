---
"@kubb/core": patch
---

Bump `@kubb/fabric-core` and `@kubb/react-fabric` to 0.16.1, which fixes two high severity `ws` vulnerabilities (uninitialized memory disclosure and memory exhaustion DoS).
