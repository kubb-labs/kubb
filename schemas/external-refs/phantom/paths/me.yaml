get:
  operationId: getMe
  summary: Get current user
  tags:
    - users
  responses:
    '200':
      description: Current user
      content:
        application/json:
          schema:
            $ref: '../components/schemas/User.yaml'
